# 智能家居系统 - 启动指南

## 当前系统状态

✅ **已准备完成**
- MySQL (Docker): 运行中 localhost:5455
- Redis (Valkey): 运行中 localhost:6379  
- 前端: 已编译 dist/

## 后端微服务启动

由于后端使用Spring Boot微服务架构，需要按顺序启动以下服务：

### 1. 启动 Eureka 注册中心
```powershell
cd smart-home-microservices\eureka-server
mvn spring-boot:run
```
访问: http://localhost:8761

### 2. 启动 API Gateway
新开终端窗口：
```powershell
cd smart-home-microservices\api-gateway
mvn spring-boot:run
```
端口: 8080

### 3. 启动业务服务（可并行）
```powershell
# 设备服务
cd smart-home-microservices\device-service
mvn spring-boot:run

# 用户服务
cd smart-home-microservices\user-service
mvn spring-boot:run

# 场景服务
cd smart-home-microservices\scene-service
mvn spring-boot:run

# 数据分析服务
cd smart-home-microservices\analytics-service
mvn spring-boot:run
```

### 4. 启动前端开发服务器
```powershell
npm run dev
```
访问: http://localhost:3000

## 使用已编译的前端

如果不想启动开发服务器，可以直接打开已编译的前端：

### 方法一：VS Code Live Server
在VS Code中安装 Live Server 插件，然后打开 dist/index.html

### 方法二：简单HTTP服务器
```powershell
cd dist
python -m http.server 3000
```
访问: http://localhost:3000

### 方法三：Nginx（推荐生产环境）
配置nginx.conf将dist目录设置为根目录

## 快速测试步骤

1. 启动 Eureka (8761)
2. 启动 API Gateway (8080)
3. 启动 device-service (8081)
4. 启动 user-service (8082)
5. 启动前端 (3000)
6. 打开浏览器访问 http://localhost:3000

## 登录信息

默认测试账号：
- 用户名: admin
- 密码: password

或使用数据库中的测试账号

## 常见问题

### Q: 端口被占用
```powershell
# 查看端口占用
netstat -ano | findstr <端口号>

# 结束进程
taskkill /PID <进程ID> /F
```

### Q: 数据库连接失败
检查MySQL是否运行：
```powershell
docker ps | findstr mysql
```

### Q: 前端无法访问后端
检查API Gateway是否启动：
```powershell
netstat -ano | findstr 8080
```

## 系统架构

```
┌─────────────┐
│  前端 (3000) │
└──────┬──────┘
       │ /api
       ▼
┌─────────────┐
│ API Gateway │
│   (8080)    │
└──────┬──────┘
       │
       ├──────────────────────┐
       │                      │
       ▼                      ▼
┌─────────────┐          ┌─────────────┐
│   Eureka   │          │ Device Svc  │
│  (8761)    │          │   (8081)   │
└─────────────┘          └─────────────┘
       │
       ▼
┌─────────────┐
│   MySQL    │
│  (5455)   │
└─────────────┘
       │
       ▼
┌─────────────┐
│   Redis    │
│  (6379)   │
└─────────────┘
```
