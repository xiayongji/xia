# 智能家居云边协同系统数据库设计说明

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 智能家居云边协同系统 |
| 数据库类型 | MySQL 8.0 |
| 数据库数量 | 7个 |
| 数据表总数 | 25个 |
| 文档版本 | 1.0 |
| 创建日期 | 2026-05-18 |

---

## 目录

1. [数据库概览](#一数据库概览)
2. [数据库连接配置](#二数据库连接配置)
3. [边缘网关数据库 (smarthome_edge)](#三边缘网关数据库-smarthome_edge)
4. [设备管理数据库 (smarthome_device)](#四设备管理数据库-smarthome_device)
5. [场景管理数据库 (smarthome_scene)](#五场景管理数据库-smarthome_scene)
6. [用户管理数据库 (smarthome_user)](#六用户管理数据库-smarthome_user)
7. [数据分析数据库 (smarthome_analytics)](#七数据分析数据库-smarthome_analytics)
8. [多模态数据库 (smarthome_multimodal)](#八多模态数据库-smarthome_multimodal)
9. [数据库关系图](#九数据库关系图)
10. [快速查询示例](#十快速查询示例)

---

## 一、数据库概览

### 1.1 数据库列表

| 序号 | 数据库名 | 用途 | 主要服务 |
|------|---------|------|---------|
| 1 | smarthome_edge | 边缘网关数据 | edge-service |
| 2 | smarthome_device | 设备管理 | device-service |
| 3 | smarthome_scene | 场景自动化 | scene-service |
| 4 | smarthome_user | 用户管理 | user-service |
| 5 | smarthome_analytics | 数据分析 | analytics-service |
| 6 | smarthome_multimodal | 多模态交互 | multimodal-service |
| 7 | smart_home | 综合主数据库 | API Gateway |

### 1.2 连接配置

```
主机: localhost
端口: 5455
用户名: root
密码: infini_rag_flow
字符集: utf8mb4
```

### 1.3 Navicat连接

在Navicat中新建连接：
- 连接类型：MySQL
- 主机：localhost
- 端口：5455
- 用户名：root
- 密码：infini_rag_flow

---

## 二、数据库连接配置

### 2.1 各微服务配置

#### Edge服务 (边缘网关)
```yaml
数据库: smarthome_edge
端口: 5455
用户名: root
密码: infini_rag_flow
服务端口: 8084
```

#### Device服务 (设备管理)
```yaml
数据库: smarthome_device
端口: 5455
用户名: root
密码: infini_rag_flow
服务端口: 8082
```

#### Scene服务 (场景管理)
```yaml
数据库: smarthome_scene
端口: 5455
用户名: root
密码: infini_rag_flow
服务端口: 8083
```

#### User服务 (用户管理)
```yaml
数据库: smarthome_user
端口: 5455
用户名: root
密码: infini_rag_flow
服务端口: 8081
```

#### Analytics服务 (数据分析)
```yaml
数据库: smarthome_analytics
端口: 5455
用户名: root
密码: infini_rag_flow
服务端口: 8086
```

#### Multimodal服务 (多模态)
```yaml
数据库: smarthome_multimodal
端口: 5455
用户名: root
密码: infini_rag_flow
服务端口: 8087
```

---

## 三、边缘网关数据库 (smarthome_edge)

**用途**: 存储边缘网关管理的本地设备信息和实时状态

**特点**:
- 支持离线快速响应
- 毫秒级设备控制
- 本地数据缓存
- 云边数据同步

### 表3.1 edge_device - 边缘设备表

#### 作用
存储所有连接到边缘网关的设备基本信息，包括设备标识、类型、协议、位置等。

#### 表结构
```sql
CREATE TABLE edge_device (
    device_id VARCHAR(64) PRIMARY KEY COMMENT '设备唯一标识',
    device_name VARCHAR(128) COMMENT '设备名称',
    device_type VARCHAR(64) COMMENT '设备类型',
    protocol VARCHAR(32) COMMENT '通信协议: WiFi/Bluetooth/ZigBee',
    status VARCHAR(32) COMMENT '设备状态: online/offline/error',
    location VARCHAR(128) COMMENT '设备位置',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_status (status),
    INDEX idx_device_type (device_type),
    INDEX idx_protocol (protocol)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| device_id | VARCHAR(64) | 是 | 设备唯一标识（主键） | device-001 |
| device_name | VARCHAR(128) | 否 | 设备显示名称 | 智能灯泡-客厅 |
| device_type | VARCHAR(64) | 否 | 设备类型 | light, socket, sensor, lock, ac |
| protocol | VARCHAR(32) | 否 | 通信协议 | WiFi, Bluetooth, ZigBee |
| status | VARCHAR(32) | 否 | 当前状态 | online, offline, error |
| location | VARCHAR(128) | 否 | 安装位置 | living_room, bedroom |
| created_at | DATETIME | 否 | 创建时间 | 2026-05-18 10:00:00 |
| updated_at | DATETIME | 否 | 更新时间 | 2026-05-18 12:00:00 |

#### 示例数据
```sql
INSERT INTO edge_device (device_id, device_name, device_type, protocol, status, location) VALUES
('device-001', '智能灯泡-客厅', 'light', 'WiFi', 'online', 'living_room'),
('device-002', '智能插座-卧室', 'socket', 'WiFi', 'online', 'bedroom'),
('device-003', '温湿度传感器', 'sensor', 'ZigBee', 'online', 'living_room'),
('device-004', '蓝牙门锁', 'lock', 'Bluetooth', 'online', 'front_door'),
('device-005', '空调控制器', 'ac', 'WiFi', 'online', 'bedroom');
```

---

### 表3.2 edge_device_status - 设备状态表

#### 作用
存储设备的实时状态数据，包括功率、温度、湿度等传感器读数，用于实时监控和数据分析。

#### 表结构
```sql
CREATE TABLE edge_device_status (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    device_id VARCHAR(64) NOT NULL COMMENT '设备ID',
    status VARCHAR(32) COMMENT '设备状态',
    device_type VARCHAR(64) COMMENT '设备类型',
    power DOUBLE DEFAULT 0.0 COMMENT '功率(W)',
    temperature DOUBLE COMMENT '温度(℃)',
    humidity DOUBLE COMMENT '湿度(%)',
    properties TEXT COMMENT '其他属性(JSON格式)',
    last_update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    UNIQUE KEY uk_device_id (device_id),
    FOREIGN KEY (device_id) REFERENCES edge_device(device_id) ON DELETE CASCADE,
    INDEX idx_last_update_time (last_update_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| device_id | VARCHAR(64) | 是 | 设备ID（外键） | device-001 |
| status | VARCHAR(32) | 否 | 当前状态 | online, offline |
| device_type | VARCHAR(64) | 否 | 设备类型 | light |
| power | DOUBLE | 否 | 实时功率(W) | 15.5 |
| temperature | DOUBLE | 否 | 环境温度(℃) | 25.3 |
| humidity | DOUBLE | 否 | 环境湿度(%) | 65.2 |
| properties | TEXT | 否 | 其他属性JSON | {"brightness":80} |
| last_update_time | DATETIME | 否 | 最后更新时间 | 2026-05-18 12:00:00 |
| created_at | DATETIME | 否 | 创建时间 | 2026-05-18 10:00:00 |

#### 示例数据
```sql
INSERT INTO edge_device_status (device_id, status, device_type, power, temperature, humidity, properties) VALUES
('device-001', 'online', 'light', 15.5, NULL, NULL, '{"brightness": 80, "color": "warm"}'),
('device-002', 'online', 'socket', 120.0, NULL, NULL, '{"voltage": 220, "current": 0.5}'),
('device-003', 'online', 'sensor', 0.1, 25.3, 65.2, '{"accuracy": 0.5}'),
('device-004', 'online', 'lock', 0.2, NULL, NULL, '{"battery": 85}'),
('device-005', 'online', 'ac', 800.0, 24.0, NULL, '{"mode": "cooling", "fan_speed": "auto"}');
```

---

### 表3.3 edge_command_history - 命令历史表

#### 作用
记录所有设备控制命令的执行历史，用于故障排查、性能分析和用户行为审计。

#### 表结构
```sql
CREATE TABLE edge_command_history (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    device_id VARCHAR(64) NOT NULL COMMENT '设备ID',
    command VARCHAR(255) NOT NULL COMMENT '执行的命令',
    result VARCHAR(64) COMMENT '执行结果: success/failed',
    message TEXT COMMENT '详细消息',
    response_time_ms INT COMMENT '响应时间(毫秒)',
    executed_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '执行时间',
    FOREIGN KEY (device_id) REFERENCES edge_device(device_id) ON DELETE CASCADE,
    INDEX idx_executed_at (executed_at),
    INDEX idx_result (result)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| device_id | VARCHAR(64) | 是 | 设备ID（外键） | device-001 |
| command | VARCHAR(255) | 是 | 执行的命令 | turn_on, turn_off |
| result | VARCHAR(64) | 否 | 执行结果 | success, failed |
| message | TEXT | 否 | 详细消息 | 命令执行成功 |
| response_time_ms | INT | 否 | 响应时间(ms) | 45 |
| executed_at | DATETIME | 否 | 执行时间 | 2026-05-18 12:00:00 |

---

## 四、设备管理数据库 (smarthome_device)

**用途**: 云端设备注册、状态监控、数字孪生管理

**特点**:
- 设备统一注册和管理
- 实时状态监控
- 设备影子实现离线控制
- 心跳机制保证设备在线

### 表4.1 devices - 设备表

#### 作用
存储云端管理的所有设备基本信息，包括网络配置、厂商信息、固件版本等。

#### 表结构
```sql
CREATE TABLE devices (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    device_id VARCHAR(64) UNIQUE NOT NULL COMMENT '设备唯一标识',
    name VARCHAR(100) NOT NULL COMMENT '设备名称',
    type VARCHAR(50) NOT NULL COMMENT '设备类型',
    protocol VARCHAR(30) NOT NULL COMMENT '通信协议',
    status VARCHAR(20) DEFAULT 'offline' COMMENT '设备状态',
    ip_address VARCHAR(50) COMMENT 'IP地址',
    mac_address VARCHAR(50) UNIQUE COMMENT 'MAC地址',
    firmware_version VARCHAR(50) COMMENT '固件版本',
    manufacturer VARCHAR(100) COMMENT '制造商',
    model VARCHAR(100) COMMENT '设备型号',
    created_at DATETIME NOT NULL COMMENT '创建时间',
    updated_at DATETIME COMMENT '更新时间',
    last_heartbeat DATETIME COMMENT '最后心跳时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| device_id | VARCHAR(64) | 是 | 设备唯一标识 | dev-001 |
| name | VARCHAR(100) | 是 | 设备名称 | 客厅灯 |
| type | VARCHAR(50) | 是 | 设备类型 | light |
| protocol | VARCHAR(30) | 是 | 通信协议 | WiFi |
| status | VARCHAR(20) | 否 | 设备状态 | online/offline/warning |
| ip_address | VARCHAR(50) | 否 | IP地址 | 192.168.1.100 |
| mac_address | VARCHAR(50) | 否 | MAC地址 | AA:BB:CC:DD:EE:FF |
| firmware_version | VARCHAR(50) | 否 | 固件版本 | v2.1.0 |
| manufacturer | VARCHAR(100) | 否 | 制造商 | 小米 |
| model | VARCHAR(100) | 否 | 设备型号 | MJL-001 |
| created_at | DATETIME | 是 | 创建时间 | 2026-05-18 10:00:00 |
| updated_at | DATETIME | 否 | 更新时间 | 2026-05-18 12:00:00 |
| last_heartbeat | DATETIME | 否 | 最后心跳 | 2026-05-18 12:00:00 |

---

### 表4.2 device_heartbeats - 设备心跳表

#### 作用
记录设备的心跳数据，用于实时监控设备运行状态，包括CPU、内存、温度等硬件指标。

#### 表结构
```sql
CREATE TABLE device_heartbeats (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    device_id VARCHAR(64) NOT NULL COMMENT '设备ID',
    timestamp DATETIME NOT NULL COMMENT '心跳时间',
    status VARCHAR(20) COMMENT '设备状态',
    cpu_usage DECIMAL(5,2) COMMENT 'CPU使用率(%)',
    memory_usage DECIMAL(5,2) COMMENT '内存使用率(%)',
    temperature DECIMAL(5,2) COMMENT '设备温度(℃)',
    network_status VARCHAR(20) COMMENT '网络状态'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| device_id | VARCHAR(64) | 是 | 设备ID | dev-001 |
| timestamp | DATETIME | 是 | 心跳时间 | 2026-05-18 12:00:00 |
| status | VARCHAR(20) | 否 | 设备状态 | healthy |
| cpu_usage | DECIMAL(5,2) | 否 | CPU使用率(%) | 25.5 |
| memory_usage | DECIMAL(5,2) | 否 | 内存使用率(%) | 45.2 |
| temperature | DECIMAL(5,2) | 否 | 设备温度(℃) | 38.5 |
| network_status | VARCHAR(20) | 否 | 网络状态 | connected |

---

### 表4.3 device_shadows - 设备影子表

#### 作用
实现设备数字孪生，记录期望状态和实际上报状态，支持离线控制和状态同步。

#### 表结构
```sql
CREATE TABLE device_shadows (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    device_id VARCHAR(64) UNIQUE NOT NULL COMMENT '设备ID',
    desired_state TEXT COMMENT '期望状态(JSON)',
    reported_state TEXT COMMENT '上报状态(JSON)',
    last_updated DATETIME NOT NULL COMMENT '最后更新时间',
    version INT DEFAULT 1 COMMENT '版本号'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| device_id | VARCHAR(64) | 是 | 设备ID（唯一） | dev-001 |
| desired_state | TEXT | 否 | 期望状态JSON | {"power":"on","brightness":80} |
| reported_state | TEXT | 否 | 上报状态JSON | {"power":"on","brightness":75} |
| last_updated | DATETIME | 是 | 最后更新时间 | 2026-05-18 12:00:00 |
| version | INT | 否 | 版本号 | 5 |

#### JSON状态示例
```json
// 期望状态
{
  "power": "on",
  "brightness": 80,
  "color": "warm",
  "mode": "auto"
}

// 实际上报状态
{
  "power": "on",
  "brightness": 75,
  "color": "warm",
  "mode": "auto",
  "online": true,
  "firmware": "v2.1.0"
}
```

---

## 五、场景管理数据库 (smarthome_scene)

**用途**: 智能家居自动化场景配置、规则引擎、执行记录

**特点**:
- 多条件触发规则
- 设备联动控制
- 场景执行历史
- 智能场景预测

### 表5.1 scenes - 场景表

#### 作用
存储智能家居自动化场景的基本信息，如"回家模式"、"睡眠模式"等。

#### 表结构
```sql
CREATE TABLE scenes (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    scene_id VARCHAR(64) UNIQUE NOT NULL COMMENT '场景唯一标识',
    name VARCHAR(100) NOT NULL COMMENT '场景名称',
    description VARCHAR(500) COMMENT '场景描述',
    enabled BOOLEAN DEFAULT true COMMENT '是否启用',
    created_at DATETIME NOT NULL COMMENT '创建时间',
    updated_at DATETIME COMMENT '更新时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| scene_id | VARCHAR(64) | 是 | 场景唯一标识 | scene-001 |
| name | VARCHAR(100) | 是 | 场景名称 | 回家模式 |
| description | VARCHAR(500) | 否 | 场景描述 | 打开灯光空调播放音乐 |
| enabled | BOOLEAN | 否 | 是否启用 | true |
| created_at | DATETIME | 是 | 创建时间 | 2026-05-18 10:00:00 |
| updated_at | DATETIME | 否 | 更新时间 | 2026-05-18 12:00:00 |

#### 示例数据
```sql
INSERT INTO scenes (scene_id, name, description, enabled) VALUES
('scene-001', '回家模式', '打开灯光、空调、播放音乐', true),
('scene-002', '睡眠模式', '关闭灯光、调低空调、安防布防', true),
('scene-003', '离家模式', '关闭所有设备、安防布防', true),
('scene-004', '观影模式', '调暗灯光、关闭窗帘、打开电视', true);
```

---

### 表5.2 scene_rules - 场景规则表

#### 作用
定义场景的触发规则，包括时间触发、设备触发、环境触发等多种条件。

#### 表结构
```sql
CREATE TABLE scene_rules (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    rule_id VARCHAR(64) UNIQUE NOT NULL COMMENT '规则唯一标识',
    type VARCHAR(30) NOT NULL COMMENT '规则类型',
    condition TEXT COMMENT '触发条件(JSON)',
    scene_id BIGINT NOT NULL COMMENT '所属场景ID',
    priority INT DEFAULT 1 COMMENT '优先级',
    enabled BOOLEAN DEFAULT true COMMENT '是否启用',
    FOREIGN KEY (scene_id) REFERENCES scenes(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| rule_id | VARCHAR(64) | 是 | 规则唯一标识 | rule-001 |
| type | VARCHAR(30) | 是 | 规则类型 | time/device/environment |
| condition | TEXT | 否 | 触发条件JSON | {"time":"18:00"} |
| scene_id | BIGINT | 是 | 所属场景ID | 1 |
| priority | INT | 否 | 优先级(1-10) | 5 |
| enabled | BOOLEAN | 否 | 是否启用 | true |

#### 规则类型说明

| 类型 | 说明 | 条件示例 |
|------|------|---------|
| time | 时间触发 | {"time": "18:00"} |
| device | 设备状态触发 | {"device_id": "door_sensor", "action": "open"} |
| environment | 环境触发 | {"temperature": ">30"} |
| location | 位置触发 | {"location": "leave_home"} |
| manual | 手动触发 | {} |

---

### 表5.3 scene_actions - 场景动作表

#### 作用
定义场景执行时要执行的设备和命令，支持延时执行和顺序控制。

#### 表结构
```sql
CREATE TABLE scene_actions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    action_id VARCHAR(64) UNIQUE NOT NULL COMMENT '动作唯一标识',
    device_id VARCHAR(64) NOT NULL COMMENT '目标设备ID',
    command VARCHAR(100) NOT NULL COMMENT '执行命令',
    parameters TEXT COMMENT '命令参数(JSON)',
    order_index INT DEFAULT 0 COMMENT '执行顺序',
    delay_seconds INT DEFAULT 0 COMMENT '延迟秒数',
    type VARCHAR(30) DEFAULT 'device_control' COMMENT '动作类型',
    enabled BOOLEAN DEFAULT true COMMENT '是否启用',
    FOREIGN KEY (rule_id) REFERENCES scene_rules(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| action_id | VARCHAR(64) | 是 | 动作唯一标识 | action-001 |
| device_id | VARCHAR(64) | 是 | 目标设备ID | device-001 |
| command | VARCHAR(100) | 是 | 执行命令 | turn_on |
| parameters | TEXT | 否 | 命令参数JSON | {"brightness":80} |
| order_index | INT | 否 | 执行顺序 | 1 |
| delay_seconds | INT | 否 | 延迟秒数 | 0 |
| type | VARCHAR(30) | 否 | 动作类型 | device_control |
| enabled | BOOLEAN | 否 | 是否启用 | true |

#### 示例数据
```sql
-- 回家模式的动作
INSERT INTO scene_actions (action_id, device_id, command, parameters, order_index, delay_seconds) VALUES
('action-001', 'device-001', 'turn_on', '{"brightness": 80}', 1, 0),
('action-002', 'device-005', 'turn_on', '{"temperature": 24}', 2, 5),
('action-003', 'device-speaker', 'play_music', '{"playlist": "relax"}', 3, 10);
```

---

### 表5.4 scene_executions - 场景执行记录表

#### 作用
记录场景的执行历史，包括触发条件、执行状态、动作结果等，用于审计和故障排查。

#### 表结构
```sql
CREATE TABLE scene_executions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    scene_id VARCHAR(64) NOT NULL COMMENT '场景ID',
    trigger_type VARCHAR(50) COMMENT '触发类型',
    trigger_condition TEXT COMMENT '触发条件',
    status VARCHAR(20) DEFAULT 'running' COMMENT '执行状态',
    total_actions INT DEFAULT 0 COMMENT '总动作数',
    success_actions INT DEFAULT 0 COMMENT '成功动作数',
    failed_actions INT DEFAULT 0 COMMENT '失败动作数',
    execution_log TEXT COMMENT '执行日志',
    start_time DATETIME NOT NULL COMMENT '开始时间',
    end_time DATETIME COMMENT '结束时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| scene_id | VARCHAR(64) | 是 | 场景ID | scene-001 |
| trigger_type | VARCHAR(50) | 否 | 触发类型 | time/manual/device |
| trigger_condition | TEXT | 否 | 触发条件 | {"time":"18:00"} |
| status | VARCHAR(20) | 否 | 执行状态 | running/success/failed |
| total_actions | INT | 否 | 总动作数 | 3 |
| success_actions | INT | 否 | 成功动作数 | 3 |
| failed_actions | INT | 否 | 失败动作数 | 0 |
| execution_log | TEXT | 否 | 执行日志 | 详细日志信息 |
| start_time | DATETIME | 是 | 开始时间 | 2026-05-18 18:00:00 |
| end_time | DATETIME | 否 | 结束时间 | 2026-05-18 18:00:15 |

---

### 表5.5 predicted_scenes - 预测场景表

#### 作用
基于用户行为分析，预测用户即将执行的场景，用于智能推荐和主动服务。

#### 表结构
```sql
CREATE TABLE predicted_scenes (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id VARCHAR(64) NOT NULL COMMENT '用户ID',
    scene_id VARCHAR(64) NOT NULL COMMENT '预测场景ID',
    predicted_time DATETIME NOT NULL COMMENT '预测执行时间',
    confidence DECIMAL(5,2) NOT NULL COMMENT '置信度',
    actual_executed BOOLEAN COMMENT '是否实际执行',
    predicted_at DATETIME NOT NULL COMMENT '预测时间',
    INDEX idx_user_id (user_id),
    INDEX idx_predicted_time (predicted_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| user_id | VARCHAR(64) | 是 | 用户ID | user-001 |
| scene_id | VARCHAR(64) | 是 | 预测场景ID | scene-001 |
| predicted_time | DATETIME | 是 | 预测执行时间 | 2026-05-18 18:00:00 |
| confidence | DECIMAL(5,2) | 是 | 置信度(0-100) | 85.5 |
| actual_executed | BOOLEAN | 否 | 是否实际执行 | true |
| predicted_at | DATETIME | 是 | 预测时间 | 2026-05-18 10:00:00 |

---

## 六、用户管理数据库 (smarthome_user)

**用途**: 用户认证、授权、个性化设置、操作审计

**特点**:
- RBAC权限管理
- 完整操作日志
- 个性化偏好设置
- 多因素认证支持

### 表6.1 users - 用户表

#### 作用
存储系统用户的基本信息，包括认证凭证和个人资料。

#### 表结构
```sql
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL COMMENT '用户名',
    password VARCHAR(255) NOT NULL COMMENT '加密密码',
    email VARCHAR(100) UNIQUE COMMENT '邮箱',
    phone VARCHAR(20) COMMENT '手机号',
    full_name VARCHAR(100) COMMENT '全名',
    role_id BIGINT COMMENT '角色ID',
    enabled BOOLEAN DEFAULT true COMMENT '是否启用',
    created_at DATETIME NOT NULL COMMENT '创建时间',
    updated_at DATETIME COMMENT '更新时间',
    last_login DATETIME COMMENT '最后登录时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| username | VARCHAR(50) | 是 | 用户名（唯一） | admin |
| password | VARCHAR(255) | 是 | 加密密码 | $2a$10$... |
| email | VARCHAR(100) | 否 | 邮箱（唯一） | admin@example.com |
| phone | VARCHAR(20) | 否 | 手机号 | 13800138000 |
| full_name | VARCHAR(100) | 否 | 全名 | 张三 |
| role_id | BIGINT | 否 | 角色ID | 1 |
| enabled | BOOLEAN | 否 | 是否启用 | true |
| created_at | DATETIME | 是 | 创建时间 | 2026-05-18 10:00:00 |
| updated_at | DATETIME | 否 | 更新时间 | 2026-05-18 12:00:00 |
| last_login | DATETIME | 否 | 最后登录 | 2026-05-18 12:00:00 |

---

### 表6.2 user_roles - 用户角色表

#### 作用
定义系统角色，如管理员、普通用户、访客等。

#### 表结构
```sql
CREATE TABLE user_roles (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    role_name VARCHAR(50) UNIQUE NOT NULL COMMENT '角色名称',
    description VARCHAR(200) COMMENT '角色描述'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| role_name | VARCHAR(50) | 是 | 角色名称（唯一） | admin |
| description | VARCHAR(200) | 否 | 角色描述 | 系统管理员 |

#### 示例数据
```sql
INSERT INTO user_roles (role_name, description) VALUES
('admin', '系统管理员，拥有所有权限'),
('user', '普通用户，可管理自己的设备'),
('guest', '访客，仅可查看');
```

---

### 表6.3 user_permissions - 权限表

#### 作用
定义细粒度的权限，控制用户对系统资源的访问和操作。

#### 表结构
```sql
CREATE TABLE user_permissions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    permission_name VARCHAR(100) UNIQUE NOT NULL COMMENT '权限名称',
    description VARCHAR(200) COMMENT '权限描述',
    resource VARCHAR(100) COMMENT '资源',
    action VARCHAR(50) COMMENT '操作'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| permission_name | VARCHAR(100) | 是 | 权限名称（唯一） | device:read |
| description | VARCHAR(200) | 否 | 权限描述 | 读取设备信息 |
| resource | VARCHAR(100) | 否 | 资源类型 | device |
| action | VARCHAR(50) | 否 | 操作类型 | read |

#### 示例数据
```sql
INSERT INTO user_permissions (permission_name, description, resource, action) VALUES
('device:read', '读取设备信息', 'device', 'read'),
('device:control', '控制设备', 'device', 'control'),
('device:manage', '管理设备', 'device', 'manage'),
('scene:execute', '执行场景', 'scene', 'execute'),
('scene:create', '创建场景', 'scene', 'create'),
('user:manage', '管理用户', 'user', 'manage');
```

---

### 表6.4 role_permissions - 角色权限关联表

#### 作用
建立角色和权限的多对多关系，实现灵活的权限分配。

#### 表结构
```sql
CREATE TABLE role_permissions (
    role_id BIGINT NOT NULL COMMENT '角色ID',
    permission_id BIGINT NOT NULL COMMENT '权限ID',
    PRIMARY KEY (role_id, permission_id),
    FOREIGN KEY (role_id) REFERENCES user_roles(id) ON DELETE CASCADE,
    FOREIGN KEY (permission_id) REFERENCES user_permissions(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 表6.5 user_settings - 用户设置表

#### 作用
存储用户的个性化偏好设置，包括界面主题、语言、通知配置等。

#### 表结构
```sql
CREATE TABLE user_settings (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE NOT NULL COMMENT '用户ID',
    theme VARCHAR(20) DEFAULT 'light' COMMENT '主题',
    language VARCHAR(10) DEFAULT 'zh-CN' COMMENT '语言',
    timezone VARCHAR(50) DEFAULT 'Asia/Shanghai' COMMENT '时区',
    notifications_enabled BOOLEAN DEFAULT true COMMENT '通知开关',
    email_notifications BOOLEAN DEFAULT true COMMENT '邮件通知',
    push_notifications BOOLEAN DEFAULT true COMMENT '推送通知',
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| user_id | BIGINT | 是 | 用户ID（唯一） | 1 |
| theme | VARCHAR(20) | 否 | 界面主题 | light/dark |
| language | VARCHAR(10) | 否 | 语言 | zh-CN/en |
| timezone | VARCHAR(50) | 否 | 时区 | Asia/Shanghai |
| notifications_enabled | BOOLEAN | 否 | 通知总开关 | true |
| email_notifications | BOOLEAN | 否 | 邮件通知 | true |
| push_notifications | BOOLEAN | 否 | 推送通知 | true |

---

### 表6.6 operation_logs - 操作日志表

#### 作用
记录用户在系统中的所有操作，用于安全审计和故障排查。

#### 表结构
```sql
CREATE TABLE operation_logs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL COMMENT '操作用户',
    operation VARCHAR(100) NOT NULL COMMENT '操作类型',
    resource VARCHAR(100) COMMENT '操作资源',
    ip_address VARCHAR(50) COMMENT 'IP地址',
    user_agent VARCHAR(255) COMMENT '用户代理',
    success BOOLEAN DEFAULT true COMMENT '是否成功',
    error_message TEXT COMMENT '错误信息',
    created_at DATETIME NOT NULL COMMENT '操作时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| username | VARCHAR(50) | 是 | 操作用户 | admin |
| operation | VARCHAR(100) | 是 | 操作类型 | device:control |
| resource | VARCHAR(100) | 否 | 操作资源 | device-001 |
| ip_address | VARCHAR(50) | 否 | IP地址 | 192.168.1.100 |
| user_agent | VARCHAR(255) | 否 | 用户代理 | Mozilla/5.0... |
| success | BOOLEAN | 否 | 是否成功 | true |
| error_message | TEXT | 否 | 错误信息 | null |
| created_at | DATETIME | 是 | 操作时间 | 2026-05-18 12:00:00 |

---

## 七、数据分析数据库 (smarthome_analytics)

**用途**: 能耗分析、异常检测、用户行为分析

**特点**:
- 实时能耗监测
- 异常智能检测
- 行为模式分析
- 节能建议生成

### 表7.1 energy_consumption - 能耗数据表

#### 作用
记录设备的能耗数据，用于电费计算、节能分析和用电趋势预测。

#### 表结构
```sql
CREATE TABLE energy_consumption (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    device_id VARCHAR(64) NOT NULL COMMENT '设备ID',
    device_name VARCHAR(100) COMMENT '设备名称',
    power DECIMAL(10,3) NOT NULL COMMENT '功率(W)',
    energy DECIMAL(10,3) NOT NULL COMMENT '能耗(kWh)',
    timestamp DATETIME NOT NULL COMMENT '记录时间',
    unit VARCHAR(10) DEFAULT 'kWh' COMMENT '单位',
    device_type VARCHAR(50) COMMENT '设备类型'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| device_id | VARCHAR(64) | 是 | 设备ID | device-001 |
| device_name | VARCHAR(100) | 否 | 设备名称 | 智能插座-卧室 |
| power | DECIMAL(10,3) | 是 | 实时功率(W) | 120.5 |
| energy | DECIMAL(10,3) | 是 | 累计能耗(kWh) | 25.6 |
| timestamp | DATETIME | 是 | 记录时间 | 2026-05-18 12:00:00 |
| unit | VARCHAR(10) | 否 | 单位 | kWh |
| device_type | VARCHAR(50) | 否 | 设备类型 | socket |

---

### 表7.2 anomaly_detection - 异常检测表

#### 作用
记录设备异常检测结果，包括功耗异常、离线告警等，用于故障预警和维护提醒。

#### 表结构
```sql
CREATE TABLE anomaly_detection (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    device_id VARCHAR(64) NOT NULL COMMENT '设备ID',
    device_name VARCHAR(100) COMMENT '设备名称',
    anomaly_type VARCHAR(50) NOT NULL COMMENT '异常类型',
    confidence DECIMAL(5,2) NOT NULL COMMENT '置信度',
    details TEXT COMMENT '异常详情',
    status VARCHAR(20) DEFAULT 'pending' COMMENT '处理状态',
    detected_at DATETIME NOT NULL COMMENT '检测时间',
    resolved_at DATETIME COMMENT '处理时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| device_id | VARCHAR(64) | 是 | 设备ID | device-001 |
| device_name | VARCHAR(100) | 否 | 设备名称 | 智能插座-卧室 |
| anomaly_type | VARCHAR(50) | 是 | 异常类型 | power_anomaly |
| confidence | DECIMAL(5,2) | 是 | 置信度(0-100) | 95.5 |
| details | TEXT | 否 | 异常详情 | 功耗突增200% |
| status | VARCHAR(20) | 否 | 处理状态 | pending/resolved |
| detected_at | DATETIME | 是 | 检测时间 | 2026-05-18 12:00:00 |
| resolved_at | DATETIME | 否 | 处理时间 | null |

#### 异常类型说明

| 类型 | 说明 | 示例 |
|------|------|------|
| power_anomaly | 功耗异常 | 功耗突增或骤降 |
| device_offline | 设备离线 | 长时间无心跳 |
| abnormal_behavior | 行为异常 | 非习惯时间使用设备 |
| network_issue | 网络问题 | 频繁断连 |
| temperature_high | 温度过高 | 设备过热告警 |

---

### 表7.3 user_behavior - 用户行为表

#### 作用
记录用户的行为数据，用于行为模式分析、个性化推荐和智能预测。

#### 表结构
```sql
CREATE TABLE user_behavior (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id VARCHAR(64) NOT NULL COMMENT '用户ID',
    username VARCHAR(50) COMMENT '用户名',
    behavior_type VARCHAR(50) NOT NULL COMMENT '行为类型',
    device_id VARCHAR(64) NOT NULL COMMENT '设备ID',
    device_name VARCHAR(100) COMMENT '设备名称',
    timestamp DATETIME NOT NULL COMMENT '行为时间',
    action VARCHAR(100) COMMENT '执行动作',
    category VARCHAR(50) COMMENT '行为分类'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| id | BIGINT | 是 | 主键ID（自增） | 1 |
| user_id | VARCHAR(64) | 是 | 用户ID | user-001 |
| username | VARCHAR(50) | 否 | 用户名 | admin |
| behavior_type | VARCHAR(50) | 是 | 行为类型 | device_control |
| device_id | VARCHAR(64) | 是 | 设备ID | device-001 |
| device_name | VARCHAR(100) | 否 | 设备名称 | 智能灯泡-客厅 |
| timestamp | DATETIME | 是 | 行为时间 | 2026-05-18 18:00:00 |
| action | VARCHAR(100) | 否 | 执行动作 | turn_on |
| category | VARCHAR(50) | 否 | 行为分类 | lighting |

---

## 八、多模态数据库 (smarthome_multimodal)

**用途**: 传感器数据、多模态交互、上下文理解、意图识别

**特点**:
- 多传感器融合
- 上下文感知
- 意图智能识别
- 主动智能服务

### 表8.1 sensor_events - 传感器事件表

#### 作用
存储传感器产生的各类事件，如门磁触发、人体感应、温湿度变化等。

#### 表结构
```sql
CREATE TABLE sensor_events (
    event_id VARCHAR(64) PRIMARY KEY COMMENT '事件ID',
    sensor_type VARCHAR(50) NOT NULL COMMENT '传感器类型',
    event_type VARCHAR(100) NOT NULL COMMENT '事件类型',
    device_id VARCHAR(64) NOT NULL COMMENT '设备ID',
    user_id VARCHAR(64) COMMENT '用户ID',
    event_data TEXT COMMENT '事件数据(JSON)',
    confidence DECIMAL(5,2) COMMENT '置信度',
    timestamp DATETIME NOT NULL COMMENT '发生时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| event_id | VARCHAR(64) | 是 | 事件ID（主键） | event-001 |
| sensor_type | VARCHAR(50) | 是 | 传感器类型 | door_motion |
| event_type | VARCHAR(100) | 是 | 事件类型 | door_opened |
| device_id | VARCHAR(64) | 是 | 设备ID | device-sensor-001 |
| user_id | VARCHAR(64) | 否 | 用户ID | user-001 |
| event_data | TEXT | 否 | 事件数据JSON | {"duration":5} |
| confidence | DECIMAL(5,2) | 否 | 置信度 | 98.5 |
| timestamp | DATETIME | 是 | 发生时间 | 2026-05-18 12:00:00 |

---

### 表8.2 sensor_readings - 传感器读数表

#### 作用
存储传感器的原始读数数据，包括各种环境监测数据。

#### 表结构
```sql
CREATE TABLE sensor_readings (
    reading_id VARCHAR(64) PRIMARY KEY COMMENT '读数ID',
    sensor_type VARCHAR(50) NOT NULL COMMENT '传感器类型',
    device_id VARCHAR(64) NOT NULL COMMENT '设备ID',
    value DECIMAL NOT NULL COMMENT '读数值',
    unit VARCHAR(20) NOT NULL COMMENT '单位',
    quality VARCHAR(20) COMMENT '数据质量',
    timestamp DATETIME NOT NULL COMMENT '记录时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| reading_id | VARCHAR(64) | 是 | 读数ID（主键） | reading-001 |
| sensor_type | VARCHAR(50) | 是 | 传感器类型 | temperature |
| device_id | VARCHAR(64) | 是 | 设备ID | device-sensor-001 |
| value | DECIMAL | 是 | 读数值 | 25.3 |
| unit | VARCHAR(20) | 是 | 单位 | ℃ |
| quality | VARCHAR(20) | 否 | 数据质量 | good |
| timestamp | DATETIME | 是 | 记录时间 | 2026-05-18 12:00:00 |

---

### 表8.3 context_snapshots - 上下文快照表

#### 作用
存储某一时刻的完整环境上下文，用于场景理解和智能决策。

#### 表结构
```sql
CREATE TABLE context_snapshots (
    snapshot_id VARCHAR(64) PRIMARY KEY COMMENT '快照ID',
    user_id VARCHAR(64) NOT NULL COMMENT '用户ID',
    context_data TEXT COMMENT '上下文数据(JSON)',
    environment_state TEXT COMMENT '环境状态(JSON)',
    device_states TEXT COMMENT '设备状态(JSON)',
    timestamp DATETIME NOT NULL COMMENT '快照时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| snapshot_id | VARCHAR(64) | 是 | 快照ID（主键） | snapshot-001 |
| user_id | VARCHAR(64) | 是 | 用户ID | user-001 |
| context_data | TEXT | 否 | 上下文数据JSON | {"location":"home"} |
| environment_state | TEXT | 否 | 环境状态JSON | {"temp":25} |
| device_states | TEXT | 否 | 设备状态JSON | {"light":"on"} |
| timestamp | DATETIME | 是 | 快照时间 | 2026-05-18 12:00:00 |

---

### 表8.4 intent_patterns - 意图模式表

#### 作用
存储用户交互意图的模式数据，用于意图识别和智能服务推荐。

#### 表结构
```sql
CREATE TABLE intent_patterns (
    pattern_id VARCHAR(64) PRIMARY KEY COMMENT '模式ID',
    input_type VARCHAR(50) NOT NULL COMMENT '输入类型',
    intent_type VARCHAR(100) NOT NULL COMMENT '意图类型',
    pattern_data TEXT COMMENT '模式数据(JSON)',
    confidence DECIMAL(5,2) COMMENT '置信度',
    user_id VARCHAR(64) COMMENT '用户ID'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| pattern_id | VARCHAR(64) | 是 | 模式ID（主键） | pattern-001 |
| input_type | VARCHAR(50) | 是 | 输入类型 | voice/gesture/touch |
| intent_type | VARCHAR(100) | 是 | 意图类型 | control_light |
| pattern_data | TEXT | 否 | 模式数据JSON | {"keywords":["开灯"]} |
| confidence | DECIMAL(5,2) | 否 | 置信度 | 95.5 |
| user_id | VARCHAR(64) | 否 | 用户ID | user-001 |

---

### 表8.5 behavior_events - 行为事件表

#### 作用
记录用户的行为事件，用于行为分析和智能预测。

#### 表结构
```sql
CREATE TABLE behavior_events (
    event_id VARCHAR(64) PRIMARY KEY COMMENT '事件ID',
    user_id VARCHAR(64) NOT NULL COMMENT '用户ID',
    behavior_type VARCHAR(100) NOT NULL COMMENT '行为类型',
    event_data TEXT COMMENT '事件数据(JSON)',
    predicted_intent VARCHAR(100) COMMENT '预测意图',
    confidence DECIMAL(5,2) COMMENT '置信度',
    timestamp DATETIME NOT NULL COMMENT '事件时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 字段说明

| 字段名 | 数据类型 | 是否必填 | 说明 | 示例值 |
|--------|----------|----------|------|--------|
| event_id | VARCHAR(64) | 是 | 事件ID（主键） | behavior-001 |
| user_id | VARCHAR(64) | 是 | 用户ID | user-001 |
| behavior_type | VARCHAR(100) | 是 | 行为类型 | enter_room |
| event_data | TEXT | 否 | 事件数据JSON | {"room":"living"} |
| predicted_intent | VARCHAR(100) | 否 | 预测意图 | turn_on_light |
| confidence | DECIMAL(5,2) | 否 | 置信度 | 88.5 |
| timestamp | DATETIME | 是 | 事件时间 | 2026-05-18 12:00:00 |

---

## 九、数据库关系图

### 9.1 总体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          智能家居云边协同系统                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌────────────────┐         ┌────────────────┐                         │
│  │   用户管理      │         │   设备管理      │                         │
│  │ smarthome_user │◄───────►│smarthome_device│                         │
│  └───────┬────────┘         └───────┬────────┘                         │
│          │                          │                                   │
│          │                          │                                   │
│          ▼                          ▼                                   │
│  ┌────────────────┐         ┌────────────────┐                        │
│  │   场景管理      │         │   数据分析      │                        │
│  │ smarthome_scene│◄───────►│smarthome_analytics│                      │
│  └───────┬────────┘         └───────┬────────┘                        │
│          │                          │                                   │
│          │                          │                                   │
│          ▼                          ▼                                   │
│  ┌────────────────┐         ┌────────────────┐                        │
│  │   多模态数据    │         │   边缘网关      │                        │
│  │smarthome_multimodal│     │smarthome_edge │                        │
│  └────────────────┘         └───────┬────────┘                        │
│                                      │                                  │
└──────────────────────────────────────┼──────────────────────────────────┘
                                       │
                                       ▼
                          ┌────────────────────┐
                          │    智能预测        │
                          │ predicted_scenes   │
                          └────────────────────┘
```

### 9.2 详细ER图

```
users (用户)
  │
  ├──1:N──► user_roles (角色)
  │              │
  │              └──N:N──► user_permissions (权限)
  │
  ├──1:1──► user_settings (设置)
  │
  └──1:N──► operation_logs (日志)
                  │
                  └──N:1──► devices (设备)
                              │
                              ├──1:N──► device_heartbeats (心跳)
                              │
                              ├──1:1──► device_shadows (影子)
                              │
                              └──1:N──► energy_consumption (能耗)
                                              │
                                              └──► anomaly_detection (异常)

scenes (场景)
  │
  ├──1:N──► scene_rules (规则)
  │              │
  │              └──1:N──► scene_actions (动作)
  │                              │
  │                              └──N:1──► devices (设备)
  │
  └──1:N──► scene_executions (执行记录)

user_behavior (用户行为)
  │
  ├──N:1──► users (用户)
  │
  └──N:1──► devices (设备)

sensor_events (传感器事件)
  │
  └──N:1──► devices (设备)

context_snapshots (上下文快照)
  │
  ├──N:1──► users (用户)
  │
  └──N:N──► devices (设备)

behavior_events (行为事件)
  │
  └──N:1──► users (用户)
```

---

## 十、快速查询示例

### 10.1 设备相关查询

```sql
-- 查询所有在线设备
SELECT * FROM edge_device WHERE status = 'online';

-- 查询设备最新状态
SELECT ds.*, d.device_name, d.location
FROM edge_device_status ds
JOIN edge_device d ON ds.device_id = d.device_id
ORDER BY ds.last_update_time DESC;

-- 查询设备的命令历史
SELECT * FROM edge_command_history
WHERE device_id = 'device-001'
ORDER BY executed_at DESC
LIMIT 10;

-- 查询功耗排名前10的设备
SELECT device_name, power, last_update_time
FROM edge_device_status
WHERE power > 0
ORDER BY power DESC
LIMIT 10;
```

### 10.2 场景相关查询

```sql
-- 查询所有启用的场景
SELECT * FROM scenes WHERE enabled = true;

-- 查询场景的执行统计
SELECT s.name, COUNT(se.id) as execution_count,
       SUM(CASE WHEN se.status = 'success' THEN 1 ELSE 0 END) as success_count
FROM scenes s
LEFT JOIN scene_executions se ON s.scene_id = se.scene_id
GROUP BY s.id;

-- 查询今日执行的场景
SELECT * FROM scene_executions
WHERE DATE(start_time) = CURDATE()
ORDER BY start_time DESC;
```

### 10.3 能耗相关查询

```sql
-- 查询今日总能耗
SELECT SUM(energy) as total_energy
FROM energy_consumption
WHERE DATE(timestamp) = CURDATE();

-- 查询设备日能耗趋势
SELECT DATE(timestamp) as date, SUM(energy) as daily_energy
FROM energy_consumption
WHERE device_id = 'device-001'
  AND timestamp >= DATE_SUB(NOW(), INTERVAL 7 DAY)
GROUP BY DATE(timestamp);

-- 查询异常设备
SELECT * FROM anomaly_detection
WHERE status = 'pending'
ORDER BY detected_at DESC;
```

### 10.4 用户行为查询

```sql
-- 查询用户最近行为
SELECT * FROM user_behavior
WHERE user_id = 'user-001'
ORDER BY timestamp DESC
LIMIT 20;

-- 查询用户设备使用频率
SELECT device_name, COUNT(*) as usage_count
FROM user_behavior
WHERE user_id = 'user-001'
  AND timestamp >= DATE_SUB(NOW(), INTERVAL 30 DAY)
GROUP BY device_id;
```

---

## 附录

### A. 数据库维护

```sql
-- 优化表
OPTIMIZE TABLE edge_device;
OPTIMIZE TABLE edge_device_status;

-- 清理30天前的日志
DELETE FROM operation_logs
WHERE created_at < DATE_SUB(NOW(), INTERVAL 30 DAY);

-- 清理90天前的传感器数据
DELETE FROM sensor_readings
WHERE timestamp < DATE_SUB(NOW(), INTERVAL 90 DAY);
```

### B. 备份与恢复

```bash
# 备份单个数据库
mysqldump -u root -p infini_rag_flow smarthome_edge > backup_edge.sql

# 备份所有数据库
mysqldump -u root -p infini_rag_flow --all-databases > backup_all.sql

# 恢复数据库
mysql -u root -p infini_rag_flow smarthome_edge < backup_edge.sql
```

### C. 性能监控

```sql
-- 查看数据库大小
SELECT table_schema as database_name,
       ROUND(SUM(data_length + index_length) / 1024 / 1024, 2) as size_mb
FROM information_schema.tables
WHERE table_schema IN ('smarthome_edge', 'smarthome_device', 'smarthome_scene',
                       'smarthome_user', 'smarthome_analytics', 'smarthome_multimodal')
GROUP BY table_schema;

-- 查看慢查询
SHOW VARIABLES LIKE 'slow_query_log';
SHOW VARIABLES LIKE 'long_query_time';
```

---

## 文档结束

本数据库设计文档详细记录了智能家居云边协同系统的所有数据表结构、字段说明和示例数据。如有问题，请联系系统开发团队。

**版本**: 1.0
**最后更新**: 2026-05-18
**维护者**: 系统开发团队
