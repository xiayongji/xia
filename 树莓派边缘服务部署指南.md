# 树莓派边缘服务部署详细指南

## 目录
1. [环境准备](#环境准备)
2. [代码拉取](#代码拉取)
3. [依赖安装](#依赖安装)
4. [数据库配置](#数据库配置)
5. [编译打包](#编译打包)
6. [服务部署](#服务部署)
7. [验证测试](#验证测试)
8. [常见问题](#常见问题)

---

## 环境准备

### 1.1 树莓派系统要求
- **推荐系统**: Raspberry Pi OS (Debian 12 Bookworm)
- **最低配置**: 树莓派 4B 2GB 及以上
- **存储空间**: 至少 8GB

### 1.2 更新系统软件包

```bash
# 更新包列表
sudo apt update

# 升级所有已安装包
sudo apt upgrade -y

# 重启树莓派（可选，如有内核更新）
sudo reboot
```

---

## 代码拉取

### 2.1 安装 Git

```bash
# 安装 Git
sudo apt install git -y

# 验证安装
git --version
```

### 2.2 克隆代码仓库

```bash
# 创建项目目录
mkdir -p ~/smarthome
cd ~/smarthome

# 克隆仓库
git clone https://github.com/xiayongji/xia.git

# 进入边缘服务目录
cd xia/smart-home-microservices/edge-service
```

---

## 依赖安装

### 3.1 安装 Java 17

```bash
# 安装 OpenJDK 17
sudo apt install openjdk-17-jdk -y

# 验证 Java 版本
java -version
javac -version

# 配置 JAVA_HOME（可选）
echo 'export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-arm64' >> ~/.bashrc
source ~/.bashrc
```

### 3.2 安装 Maven

```bash
# 下载 Maven（树莓派使用 ARM64 版本）
cd ~/
wget https://dlcdn.apache.org/maven/maven-3/3.9.6/binaries/apache-maven-3.9.6-bin.tar.gz

# 解压
tar -xzf apache-maven-3.9.6-bin.tar.gz

# 移动到 /opt 目录
sudo mv apache-maven-3.9.6 /opt/maven

# 配置环境变量
echo 'export M2_HOME=/opt/maven' >> ~/.bashrc
echo 'export PATH=$M2_HOME/bin:$PATH' >> ~/.bashrc
source ~/.bashrc

# 验证安装
mvn -version
```

### 3.3 安装 MySQL 数据库

```bash
# 安装 MySQL Server
sudo apt install mariadb-server -y

# 启动并设置开机自启
sudo systemctl start mariadb
sudo systemctl enable mariadb

# 运行安全配置脚本（重要！）
sudo mysql_secure_installation
# 按提示操作：
# 1. 输入 root 密码（初次可直接回车）
# 2. 设置新的 root 密码（建议设为：infini_rag_flow）
# 3. 移除匿名用户：Y
# 4. 禁止 root 远程登录：Y
# 5. 移除测试数据库：Y
# 6. 重新加载权限表：Y
```

### 3.4 安装 Redis（可选）

```bash
# 安装 Redis
sudo apt install redis-server -y

# 启动并设置开机自启
sudo systemctl start redis
sudo systemctl enable redis

# 验证 Redis 运行状态
redis-cli ping
# 应返回：PONG
```

---

## 数据库配置

### 4.1 创建数据库和表

```bash
# 进入项目目录
cd ~/smarthome/xia/smart-home-microservices/edge-service

# 登录 MySQL（使用刚才设置的密码）
mysql -u root -p
```

在 MySQL 命令行中执行：

```sql
-- 执行初始化脚本
source sql/init-edge-database.sql;

-- 退出 MySQL
exit;
```

或者直接在命令行中执行：

```bash
# 直接执行初始化脚本
mysql -u root -p < sql/init-edge-database.sql
```

### 4.2 验证数据库

```bash
# 登录 MySQL
mysql -u root -p

-- 查看数据库
SHOW DATABASES;
-- 应该看到 smarthome_edge 数据库

-- 使用数据库
USE smarthome_edge;

-- 查看表
SHOW TABLES;
-- 应该看到 edge_device 和 edge_device_status 表

-- 退出
exit;
```

---

## 编译打包

### 5.1 配置 Maven 镜像（可选，加速下载）

```bash
# 编辑 Maven 配置文件
nano /opt/maven/conf/settings.xml
```

在 `<mirrors>` 标签内添加阿里云镜像：

```xml
<mirror>
  <id>aliyunmaven</id>
  <mirrorOf>*</mirrorOf>
  <name>阿里云公共仓库</name>
  <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```

### 5.2 编译项目

```bash
# 进入项目目录
cd ~/smarthome/xia/smart-home-microservices/edge-service

# 清理并打包（跳过测试，首次编译约 5-10 分钟）
mvn clean package -DskipTests

# 编译成功后，查看生成的 JAR 包
ls -lh target/edge-service-1.0.0.jar
```

---

## 服务部署

### 6.1 创建系统服务文件

```bash
# 创建 systemd 服务文件
sudo nano /etc/systemd/system/edge-service.service
```

粘贴以下内容：

```ini
[Unit]
Description=Smart Home Edge Service
After=network.target mariadb.service redis.service

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/smarthome/xia/smart-home-microservices/edge-service
ExecStart=/usr/bin/java -jar target/edge-service-1.0.0.jar
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

### 6.2 启动服务

```bash
# 重新加载 systemd 配置
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start edge-service

# 设置开机自启
sudo systemctl enable edge-service

# 查看服务状态
sudo systemctl status edge-service
```

### 6.3 查看日志

```bash
# 实时查看服务日志
sudo journalctl -u edge-service -f

# 查看最近 100 行日志
sudo journalctl -u edge-service -n 100 --no-pager
```

---

## 验证测试

### 7.1 健康检查

```bash
# 测试健康检查接口
curl http://localhost:8084/api/edge/health
```

预期返回：
```json
{
  "status": "UP"
}
```

### 7.2 测试数据库连接

```bash
# 查看所有设备状态
curl http://localhost:8084/api/edge/devices/status/all
```

### 7.3 查看统计信息

```bash
# 查看设备统计
curl http://localhost:8084/api/edge/devices/statistics
```

### 7.4 插入测试数据（可选）

```bash
# 进入项目目录
cd ~/smarthome/xia/smart-home-microservices/edge-service

# 执行测试数据脚本
mysql -u root -p < sql/insert-test-devices.sql
```

---

## 防火墙配置（如需要）

```bash
# 开放 HTTP 端口 8084
sudo ufw allow 8084/tcp

# 开放 gRPC 端口 9094（如需要）
sudo ufw allow 9094/tcp

# 查看防火墙状态
sudo ufw status
```

---

## 服务管理命令

```bash
# 启动服务
sudo systemctl start edge-service

# 停止服务
sudo systemctl stop edge-service

# 重启服务
sudo systemctl restart edge-service

# 查看状态
sudo systemctl status edge-service

# 禁用开机自启
sudo systemctl disable edge-service

# 启用开机自启
sudo systemctl enable edge-service
```

---

## 常见问题

### 问题 1：编译时内存不足

**解决方案：**
```bash
# 增加 Maven 堆内存
export MAVEN_OPTS="-Xmx1024m -Xms512m"

# 或者使用 swap 空间（临时方案）
sudo fallocate -l 1G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

### 问题 2：数据库连接失败

**检查项：**
```bash
# 1. 检查 MySQL 是否运行
sudo systemctl status mariadb

# 2. 检查数据库是否存在
mysql -u root -p -e "SHOW DATABASES;"

# 3. 检查配置文件中的端口是否正确
# 默认端口：5455（Docker）或 3306（本地安装）
```

### 问题 3：端口被占用

**解决方案：**
```bash
# 查找占用端口的进程
sudo netstat -tulpn | grep 8084

# 或者使用 lsof
sudo lsof -i :8084

# 修改 application.yml 中的端口
nano src/main/resources/application.yml
```

---

## 快速脚本（一键部署）

创建快速部署脚本：

```bash
# 创建部署脚本
nano ~/deploy-edge-service.sh
```

粘贴以下内容：

```bash
#!/bin/bash
set -e

echo "=== 树莓派边缘服务一键部署脚本 ==="

# 1. 更新系统
echo "1. 更新系统..."
sudo apt update && sudo apt upgrade -y

# 2. 安装依赖
echo "2. 安装依赖..."
sudo apt install -y git openjdk-17-jdk mariadb-server redis-server

# 3. 下载 Maven
echo "3. 安装 Maven..."
cd ~
wget -q https://dlcdn.apache.org/maven/maven-3/3.9.6/binaries/apache-maven-3.9.6-bin.tar.gz
tar -xzf apache-maven-3.9.6-bin.tar.gz
sudo mv apache-maven-3.9.6 /opt/maven
echo 'export M2_HOME=/opt/maven' >> ~/.bashrc
echo 'export PATH=$M2_HOME/bin:$PATH' >> ~/.bashrc
export M2_HOME=/opt/maven
export PATH=$M2_HOME/bin:$PATH

# 4. 克隆代码
echo "4. 克隆代码..."
mkdir -p ~/smarthome
cd ~/smarthome
if [ ! -d "xia" ]; then
    git clone https://github.com/xiayongji/xia.git
fi
cd xia/smart-home-microservices/edge-service

# 5. 初始化数据库
echo "5. 初始化数据库..."
# 注意：这里需要手动设置 MySQL root 密码
echo "请手动执行以下命令初始化数据库："
echo "  sudo mysql_secure_installation"
echo "  mysql -u root -p < sql/init-edge-database.sql"

# 6. 编译项目
echo "6. 编译项目（约 5-10 分钟）..."
mvn clean package -DskipTests

# 7. 创建系统服务
echo "7. 创建系统服务..."
sudo bash -c 'cat > /etc/systemd/system/edge-service.service << EOF
[Unit]
Description=Smart Home Edge Service
After=network.target mariadb.service redis.service

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/smarthome/xia/smart-home-microservices/edge-service
ExecStart=/usr/bin/java -jar target/edge-service-1.0.0.jar
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF'

# 8. 启动服务
echo "8. 启动服务..."
sudo systemctl daemon-reload
sudo systemctl enable edge-service
sudo systemctl start edge-service

echo ""
echo "=== 部署完成 ==="
echo "查看服务状态：sudo systemctl status edge-service"
echo "查看日志：sudo journalctl -u edge-service -f"
echo "健康检查：curl http://localhost:8084/api/edge/health"
```

添加执行权限：

```bash
chmod +x ~/deploy-edge-service.sh

# 运行脚本
~/deploy-edge-service.sh
```

---

## 维护建议

### 定期备份数据库

```bash
# 创建备份目录
mkdir -p ~/smarthome/backups

# 备份脚本
nano ~/smarthome/backup-db.sh
```

```bash
#!/bin/bash
BACKUP_DIR="/home/pi/smarthome/backups"
DATE=$(date +%Y%m%d_%H%M%S)
mysqldump -u root -p'infini_rag_flow' smarthome_edge > "$BACKUP_DIR/db_backup_$DATE.sql"
# 保留最近 7 天的备份
find $BACKUP_DIR -name "db_backup_*.sql" -mtime +7 -delete
echo "备份完成: $BACKUP_DIR/db_backup_$DATE.sql"
```

```bash
chmod +x ~/smarthome/backup-db.sh

# 添加到 crontab 每天凌晨 2 点备份
crontab -e
# 添加以下行：
# 0 2 * * * /home/pi/smarthome/backup-db.sh >> /home/pi/smarthome/backup.log 2>&1
```

---

## 技术支持

如遇到问题，请检查：
1. 服务日志：`sudo journalctl -u edge-service -f`
2. 数据库连接配置：[application.yml](file:///workspace/xia/smart-home-microservices/edge-service/src/main/resources/application.yml)
3. 网络连接和防火墙规则
4. 树莓派系统资源使用：`htop`

---

**部署完成！** 您的智能家居边缘服务现在已在树莓派上成功运行。
